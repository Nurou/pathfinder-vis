import styled from 'styled-components';
import {
  color,
  space,
  typography,
  layout,
  flexbox,
  background,
  border,
  position,
  zIndex,
  maxWidth,
  shadow,
  grid,
} from 'styled-system';

export const Box = styled.button`
  ${color}
  ${space}
  ${typography}
  ${layout}
  ${flexbox}
  ${background}
  ${border}
  ${position}
  ${zIndex}
  ${maxWidth}
  ${shadow}
  ${grid}
`;
