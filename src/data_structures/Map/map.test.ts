import { MyMap } from './index';

/* Runs before all tests */
beforeAll(() => {
  // instantiate map
  let map = new MyMap();
});

describe('the map:', () => {
  test('should accept key of type node', () => {});
});
